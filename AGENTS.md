# autobuild

Read `RULE.md` first, in full, before writing or editing any skill file.

Update `RULE.md`'s Vocabulary section only when a skill needs a new repeatable term.

The pre-commit hook bumps both plugin manifests. Leave their versions untouched during normal edits.
